/**
 * @deprecated Import from '@/shared/contexts/NotificationContext' instead.
 * This file is kept as a compatibility shim and can be deleted once all
 * references have been migrated to the canonical path.
 */
export {
    NotificationProvider,
    useNotificationContext,
    type NotificationItem,
} from '@/shared/contexts/NotificationContext';
